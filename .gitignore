node_modules/
dist/
.next/
.turbo/
coverage/
.env
.env.*
!.env.example
# Uploaded user files (runtime) — not source code
/uploads/
apps/backend/uploads/
!apps/backend/src/uploads/

# DB backups (may contain user data) and TLS private keys — never commit.
backups/*
!backups/.gitkeep
nginx/certbot/conf/*
!nginx/certbot/conf/.gitkeep
nginx/certbot/www/*
!nginx/certbot/www/.gitkeep
